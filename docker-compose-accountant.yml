version: '3.4'

services:

  accountant-portal-classic:
    image: ghcr.io/hungvutourchain/tourchain-net/hoteltourportal:latest
    networks:
      - general

networks:
  general:
    external:
      name: infrastructure_default
