version: '3.4'

services:

  accountant-portal-classic:
    image: ghcr.io/hungvutourchain/hoteltourportal:latest
    networks:
      - general

networks:
  general:
    external:
      name: infrastructure_default
